# ================================
# BACKEND CONFIGURATION
# ================================
# URL for the new AI-powered sentiment analysis backend
# Development (local)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Production (update when deploying)
# NEXT_PUBLIC_BACKEND_URL=https://your-backend-domain.com

# ================================
# FIREBASE CONFIGURATION 
# ================================
# These should already exist in your .env.local
# (No changes needed - same Firebase project)

# ================================
# SETUP NOTES
# ================================
# 1. Make sure the backend is running: npm run dev (in backend folder)
# 2. The frontend will automatically fallback to legacy API if backend is unavailable
# 3. Update NEXT_PUBLIC_BACKEND_URL when deploying to production
